managed implementation in class ZBP_R_PRU_SUMM_STRAT unique;
strict ( 2 );
with draft;
extensible;
define behavior for ZR_PRU_SUMM_STRAT alias ZrPruSummStrat
persistent table ZPRU_SUMM_STRAT
extensible
draft table ZPRU_SUMM_STRT_D
etag master LocalLastChanged
lock master total etag LastChanged
authorization master( global )
{
  field ( mandatory : create )
   SummaryStrategy;

  field ( readonly )
   LastChanged,
   LocalLastChanged;

  field ( readonly : update )
   SummaryStrategy;


  create;
  update;
  delete;

  draft action Activate optimized;
  draft action Discard;
  draft action Edit;
  draft action Resume;
  draft determine action Prepare;

  mapping for ZPRU_SUMM_STRAT corresponding extensible
  {
    SummaryStrategy = summary_strategy;
    StrategyProvider = strategy_provider;
    CreatedBy = created_by;
    CreatedAt = created_at;
    ChangedBy = changed_by;
    LastChanged = last_changed;
    LocalLastChanged = local_last_changed;
  }

}