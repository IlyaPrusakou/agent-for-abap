managed implementation in class ZBP_R_PRU_AGENT_TYPE unique;
strict ( 2 );
with draft;
extensible;
define behavior for ZR_PRU_AGENT_TYPE alias ZrPruAgentType
persistent table zpru_agent_type
extensible
draft table zpru_agnt_type_d
etag master LocalLastChanged
lock master total etag LastChanged
authorization master ( global )
{
  field ( mandatory : create )
  AgentType;

  field ( readonly )
  LastChanged,
  LocalLastChanged;

  field ( readonly : update )
  AgentType;


  create;
  update;
  delete;

  draft action Activate optimized;
  draft action Discard;
  draft action Edit;
  draft action Resume;
  draft determine action Prepare;

  mapping for zpru_agent_type corresponding extensible
    {
      AgentType        = agent_type;
      ShortMemVolume   = short_mem_volume;
      DiscardStrategy  = discard_strategy;
      SummaryStrategy  = summary_strategy;
      MaxNumbLoop      = max_numb_loop;
      CreatedBy        = created_by;
      CreatedAt        = created_at;
      ChangedBy        = changed_by;
      LastChanged      = last_changed;
      LocalLastChanged = local_last_changed;
    }

}